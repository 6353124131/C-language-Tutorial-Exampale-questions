#include <stdio.h>
// Pointer basics
int main()
{
    // int a = 76;
    // int *ptra = &a;
    // int *ptr2 = NULL;
    // printf("The Address of pointer is  %p\n", &ptra);
    // printf("The Address of 1 a is  %p\n", &a);
    // printf("The Address of a is  %p\n", &ptra);
    // printf("The Address of some garbage is  %p\n", &ptra);
    // printf("The Address of a is  %d\n", *ptr2);
    // // printf("The Address of a is  %d\n", a);
    int a = 5;
    int *p = NULL;
    printf("%d\n", p);

    return 0;
}
